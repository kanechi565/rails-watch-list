<h1>details list</h1>
<strong><%= @list.name %></strong> <br>
<%= link_to "Add bookmark", new_list_bookmark_path(@list) %>

<% @list.bookmarks.each do |bookmark| %>
  <p>
    Film : <%= bookmark.movie.title %><br>
    Commentaire : <%= bookmark.comment %>
  </p>
   <%= button_to "X", list_bookmark_path(@list, bookmark),  method: :delete,
        data: { turbo_confirm: "Es-tu sûr de vouloir supprimer ce bookmark ?" } %>
<% end %>

<%= link_to "back to list", lists_path %>
